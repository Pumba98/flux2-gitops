---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: envoy-gateway-configs
  namespace: flux-system
spec:
  patches:
    - target:
        kind: Gateway
      patch: |-
        apiVersion: gateway.networking.k8s.io/v1
        kind: Gateway
        metadata:
          name: placeholder
          annotations:
            cert-manager.io/cluster-issuer: letsencrypt-staging
