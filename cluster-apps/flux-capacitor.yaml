---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-capacitor-app
  namespace: flux-system
spec:
  path: ./apps/flux-capacitor
  targetNamespace: flux-system
  interval: 10m
  prune: true
  dependsOn:
    - name: ingress-nginx-app
  sourceRef:
    kind: GitRepository
    name: flux-system
  healthChecks:
    - apiVersion: helm.toolkit.fluxcd.io/v2beta1
      kind: HelmRelease
      name: flux-capacitor
      namespace: flux-system
