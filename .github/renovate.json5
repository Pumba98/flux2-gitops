{
  extends: [
    'config:recommended',
    ':disableRateLimiting',
    ':semanticCommits',
    ':rebaseStalePrs',
    ':separateMultipleMajorReleases',
    ':separatePatchReleases',
    'github>Pumba98/flux2-gitops//.github/renovate/commitMessage.json5',
    'github>Pumba98/flux2-gitops//.github/renovate/labels.json5',
  ],
  dependencyDashboardTitle: 'Renovate Dashboard',
  suppressNotifications: [
    'prIgnoreNotification',
  ],
  flux: {
    managerFilePatterns: [
      '/^[^\\.]*\\.ya?ml$/',
    ],
  },
  'helm-values': {
    managerFilePatterns: [
      '/^apps/[^\\.]*\\.ya?ml$/',
    ],
  },
  kubernetes: {
    managerFilePatterns: [
      '/^apps/[^\\.]*\\.ya?ml$/',
    ],
  },
  customManagers: [
    {
      customType: 'regex',
      managerFilePatterns: [
        '/^[^\\.]*\\.ya?ml$/',
      ],
      matchStrings: [
        'datasource=(?<datasource>\\S+) depName=(?<depName>.*?)\n *version: (?<currentValue>.*)\n',
      ],
      datasourceTemplate: '{{#if datasource}}{{{datasource}}}{{else}}helm{{/if}}',
    },
  ],
  packageRules: [
    {
      matchPackageNames: [
        'k3s-io/k3s',
        'ghcr.io/siderolabs/kubelet',
        'ghcr.io/siderolabs/installer',
      ],
      separateMultipleMinor: true,
    },
    {
      description: 'Use custom versioning for Minio',
      matchDatasources: [
        'docker',
      ],
      versioning: 'regex:^RELEASE\\.(?<major>\\d+)-(?<minor>\\d+)-(?<patch>\\d+)T.*Z$',
      matchPackageNames: [
        '/minio/',
      ],
    },
    {
      matchDatasources: [
        'docker',
      ],
      versioning: 'regex:^(?<major>\\d+)\\.(?<minor>\\d+)-(?<patch>\\d+)\\.(?<build>\\d+)(?:_stable)?$',
      matchPackageNames: [
        'ubuntu/bind9',
      ],
    },
    {
      matchDatasources: [
        'docker',
      ],
      versioning: 'regex:^(?<major>\\d+)-vectorchord(?<minor>\\d+)\\.(?<patch>\\d+)\\.(?<build>\\d+)$',
      commitMessageExtra: 'to {{{newValue}}}',
      matchPackageNames: [
        'ghcr.io/immich-app/postgres',
      ],
    },
    {
      groupName: 'Immich',
      matchPackageNames: [
        '/immich-.*/',
      ],
    },
    {
      groupName: 'flux-operator',
      matchPackageNames: [
        '/flux-operator/',
        '/flux-instance/'
      ],
    },
    {
      matchDatasources: [
        'docker',
      ],
      matchDepNames: [
        '!app-template',
        '!kube-prometheus-stack',
        '!weave-gitops',
        '!postgresql',
        '!redis',
        '!rancher/k3s-upgrade',
      ],
      pinDigests: true,
    },
    {
      matchDatasources: [
        'docker',
      ],
      matchPackageNames: [
        'grafana/alloy',
      ],
      pinDigests: false,
    },
    {
      matchUpdateTypes: [
        'pin',
        'pinDigest',
      ],
      automerge: true,
      automergeType: 'pr',
      automergeStrategy: 'squash',
    },
    {
      matchUpdateTypes: [
        'patch',
      ],
      automerge: true,
      automergeType: 'pr',
      automergeStrategy: 'squash',
      minimumReleaseAge: '24 hours',
      minimumReleaseAgeBehaviour: 'timestamp-optional',
    },
    {
      matchUpdateTypes: [
        'minor',
      ],
      matchPackageNames: [
        '!k3s-io/k3s',
        '!ghcr.io/siderolabs/kubelet',
        '!ghcr.io/siderolabs/installer',
        '!metallb',
        '!longhorn',
        '!rancher',
        '!/paperless-ngx/',
        '!/immich/',
      ],
      automerge: true,
      automergeType: 'pr',
      automergeStrategy: 'squash',
      minimumReleaseAge: '24 hours',
      minimumReleaseAgeBehaviour: 'timestamp-optional',
    },
    {
      matchUpdateTypes: [
        'major',
      ],
      matchPackageNames: [
        'ghcr.io/prometheus-community/charts/kube-prometheus-stack',
      ],
      automerge: true,
      automergeType: 'pr',
      automergeStrategy: 'squash',
      minimumReleaseAge: '24 hours',
      minimumReleaseAgeBehaviour: 'timestamp-optional',
    },
    {
      matchPackageNames: [
        'ubuntu/bind9',
      ],
      automerge: false,
    },
    {
      matchUpdateTypes: [
        'digest',
      ],
      matchPackageNames: [
        'ghcr.io/linuxserver/**',
        'alpine',
        'ubuntu/bind9',
      ],
      automerge: true,
      automergeType: 'pr',
      automergeStrategy: 'squash',
    },
    {
      description: 'Do not automerge minor updates for pre-1.0.0 (0.x) packages',
      matchUpdateTypes: [
        'minor',
      ],
      matchCurrentVersion: '/^v?0\\./',
      automerge: false,
    }
  ],
}
