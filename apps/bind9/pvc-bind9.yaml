---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bind9-data
spec:
  storageClassName: longhorn-backup
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 8Mi
  # dataSourceRef:
  #   kind: ReplicationDestination
  #   apiGroup: volsync.backube
  #   name: bind9-data-backup
---
apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: bind9-data-backup
spec:
  sourcePVC: bind9-data
  trigger:
    schedule: "0 * * * *"
  restic:
    pruneIntervalDays: 7
    repository: restic-config-bind9-data
    retain:
      hourly: 4
      daily: 7
      weekly: 1
    copyMethod: Clone
    storageClassName: longhorn-local
    cacheStorageClassName: longhorn-local
---
apiVersion: volsync.backube/v1alpha1
kind: ReplicationDestination
metadata:
  name: bind9-data-backup
spec:
  trigger:
    manual: restore-once
  restic:
    repository: restic-config-bind9-data
    copyMethod: Snapshot
    accessModes:
    - ReadWriteOnce
    capacity: 8Mi
    cleanupCachePVC: true
    cleanupTempPVC: true
    enableFileDeletion: true
    storageClassName: longhorn-local
    cacheStorageClassName: longhorn-local
