---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- hr-bind9.yaml
configMapGenerator:
  - name: bind9-config
    files:
      - config.yml=./resources/bind9-config.yaml
generatorOptions:
  disableNameSuffixHash: true
