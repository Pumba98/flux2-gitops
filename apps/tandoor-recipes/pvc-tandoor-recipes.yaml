---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tandoor-recipes-static
spec:
  storageClassName: longhorn-backup
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 512Mi
  dataSourceRef:
    kind: ReplicationDestination
    apiGroup: volsync.backube
    name: tandoor-recipes-static-backup
---
apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: tandoor-recipes-static-backup
spec:
  sourcePVC: tandoor-recipes-static
  trigger:
    schedule: "0 * * * *"
  restic:
    pruneIntervalDays: 7
    repository: restic-config-tandoor-recipes-static
    retain:
      hourly: 4
      daily: 7
      weekly: 1
    copyMethod: Clone
    storageClassName: longhorn-local
    cacheStorageClassName: longhorn-local
    cacheCapacity: 256Mi
---
apiVersion: volsync.backube/v1alpha1
kind: ReplicationDestination
metadata:
  name: tandoor-recipes-static-backup
spec:
  trigger:
    manual: restore-once
  restic:
    repository: restic-config-tandoor-recipes-static
    copyMethod: Snapshot
    accessModes:
    - ReadWriteOnce
    capacity: 512Mi
    cleanupCachePVC: true
    cleanupTempPVC: true
    enableFileDeletion: true
    storageClassName: longhorn-local
    cacheStorageClassName: longhorn-local
    cacheCapacity: 256Mi
