---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: immich-postgresql
spec:
  interval: 5m
  chart:
    spec:
      chart: postgresql
      version: 16.7.26
      sourceRef:
        kind: HelmRepository
        name: bitnami-charts
        namespace: flux-system
      interval: 5m
  dependsOn:
    - name: kube-prometheus-stack
      namespace: monitoring
    - name: longhorn
      namespace: longhorn-system
  values:
    global:
      security:
        allowInsecureImages: true
    image:
      registry: ghcr.io
      repository: immich-app/postgres
      tag: 17-vectorchord0.4.3@sha256:910719344d63b2c198138470e802094f7d458201d0f7408b7e1639eae47c7db9
    auth:
      username: immich
      password: "${SECRET_IMMICH_DB_PASSWORD}"
      database: immich
    primary:
      resourcesPreset: "small"
      persistence:
        enabled: true
        existingClaim: immich-postgresql-data
      podSecurityContext:
        fsGroup: 999
      containerSecurityContext:
        runAsUser: 999
        runAsGroup: 999
        readOnlyRootFilesystem: false
      # # Init Container for Major PostgreSQL Upgrades, not needed permanently
      # initContainers:
      #   - name: pgautoupgrade
      #     image: ghcr.io/pumba98/pgautoupgrade-vchord:17-debian-v0.3.0
      #     securityContext:
      #       runAsUser: 0
      #       runAsGroup: 0
      #     volumeMounts:
      #       - name: data
      #         mountPath: /bitnami/postgresql
      #     env:
      #       - name: PGAUTO_ONESHOT
      #         value: "yes"
      #       - name: POSTGRES_USER
      #         value: immich
      #       - name: POSTGRES_DB
      #         value: immich
      #       - name: PGDATA
      #         value: /bitnami/postgresql/data
    metrics:
      enabled: false
      serviceMonitor:
        enabled: true
