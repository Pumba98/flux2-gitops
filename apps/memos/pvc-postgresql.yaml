---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: memos-postgresql
spec:
  storageClassName: longhorn-backup
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 256Mi
  dataSourceRef:
    kind: ReplicationDestination
    apiGroup: volsync.backube
    name: memos-postgresql-backup
---
apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: memos-postgresql-backup
spec:
  sourcePVC: memos-postgresql
  trigger:
    schedule: "0 * * * *"
  restic:
    pruneIntervalDays: 7
    repository: restic-config-memos-postgresql
    retain:
      hourly: 4
      daily: 7
      weekly: 1
    copyMethod: Clone
    storageClassName: longhorn-local
    cacheStorageClassName: longhorn-local
    cacheCapacity: 128Mi
---
apiVersion: volsync.backube/v1alpha1
kind: ReplicationDestination
metadata:
  name: memos-postgresql-backup
spec:
  trigger:
    manual: restore-once
  restic:
    repository: restic-config-memos-postgresql
    copyMethod: Snapshot
    accessModes:
    - ReadWriteOnce
    capacity: 256Mi
    cleanupCachePVC: true
    cleanupTempPVC: true
    enableFileDeletion: true
    storageClassName: longhorn-local
    cacheStorageClassName: longhorn-local
    cacheCapacity: 128Mi
