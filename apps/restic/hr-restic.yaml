---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: restic
spec:
  interval: 5m
  chart:
    spec:
      chart: app-template
      version: 5.0.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s-charts
        namespace: flux-system
  dependsOn:
    - name: csi-driver-smb
      namespace: kube-system
  values:
    controllers:
      main:
        type: cronjob
        containers:
          main:
            image:
              repository: ghcr.io/restic/restic
              tag: 0.19.1@sha256:2f0373803493361f9304a57150d464677f69a9dad487afec202105aafb2592f2
            command:
            - /bin/sh
            - -c
            - |
              restic backup /data --exclude="downloads/*" --exclude="media/*" --exclude="staging/*" --host k3s
              restic forget --keep-daily 7 --keep-weekly 4 --keep-monthly 3 --prune
            envFrom:
            - secretRef:
                name: restic-secrets
        initContainers:
          init-repo:
            image:
              repository: ghcr.io/restic/restic
              tag: 0.19.1@sha256:2f0373803493361f9304a57150d464677f69a9dad487afec202105aafb2592f2
            command: 
            - /bin/sh
            - -c
            - |
              restic init || true
            envFrom:
            - secretRef:
                name: restic-secrets
        cronjob:
          concurrencyPolicy: Forbid
          schedule: "0 0 * * *"
          startingDeadlineSeconds: 30
          successfulJobsHistory: 1
          failedJobsHistory: 1
          backoffLimit: 3
    persistence:
      data:
        existingClaim: restic-data
        globalMounts:
          - path: "/data"
