---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: radicale
spec:
  storageClassName: longhorn-backup
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 64Mi
  dataSourceRef:
    kind: ReplicationDestination
    apiGroup: volsync.backube
    name: radicale-backup
---
apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: radicale-backup
spec:
  sourcePVC: radicale
  trigger:
    schedule: "0 * * * *"
  restic:
    pruneIntervalDays: 7
    repository: restic-config-radicale
    retain:
      hourly: 4
      daily: 7
      weekly: 1
    copyMethod: Clone
    storageClassName: longhorn-local
    cacheStorageClassName: longhorn-local
---
apiVersion: volsync.backube/v1alpha1
kind: ReplicationDestination
metadata:
  name: radicale-backup
spec:
  trigger:
    manual: restore-once
  restic:
    repository: restic-config-radicale
    copyMethod: Snapshot
    accessModes:
    - ReadWriteOnce
    capacity: 64Mi
    cleanupCachePVC: true
    cleanupTempPVC: true
    enableFileDeletion: true
    storageClassName: longhorn-local
    cacheStorageClassName: longhorn-local
