---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sftpgo-config
spec:
  storageClassName: longhorn-backup
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi
  # dataSourceRef is immutable on the existing bound PVC, so it is left as-is.
  # On a PVC recreate / disaster-recovery rebuild, delete the volsync block below
  # and restore from kopiur instead:
  #   dataSourceRef:
  #     apiGroup: kopiur.home-operations.com
  #     kind: Restore
  #     name: sftpgo-config
  dataSourceRef:
    kind: ReplicationDestination
    apiGroup: volsync.backube
    name: sftpgo-config-backup
---
apiVersion: kopiur.home-operations.com/v1alpha1
kind: SnapshotPolicy
metadata:
  name: sftpgo-config
spec:
  repository:
    kind: ClusterRepository
    name: primary
  copyMethod: Clone
  credentialProjection:
    enabled: true
  sources:
    - pvc:
        name: sftpgo-config
  retention:
    keepDaily: 7
    keepWeekly: 1
  mover:
    securityContext:
      runAsUser: 0
      runAsGroup: 0
      runAsNonRoot: false
      capabilities:
        drop: ["ALL"]
        add: ["DAC_READ_SEARCH"]
    privilegedMode: true # captures UID/GID ownership so Restore can preserve it
---
apiVersion: kopiur.home-operations.com/v1alpha1
kind: SnapshotSchedule
metadata:
  name: sftpgo-config
spec:
  policyRef:
    name: sftpgo-config
  schedule:
    cron: "0 6 * * *"
    runOnCreate: false
---
apiVersion: kopiur.home-operations.com/v1alpha1
kind: Restore
metadata:
  name: sftpgo-config
spec:
  repository:
    kind: ClusterRepository
    name: primary
  source:
    fromPolicy:
      name: sftpgo-config
  target:
    populator: {}
  credentialProjection:
    enabled: true
  mover:
    privilegedMode: true
  policy:
    onMissingSnapshot: Continue
